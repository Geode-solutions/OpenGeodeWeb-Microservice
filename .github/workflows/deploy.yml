name: Deploy

on:
  workflow_dispatch:

permissions:
  contents: write
  id-token: write

jobs:
  deploy:
    uses: Geode-solutions/actions/.github/workflows/py-deploy.yml@master
    secrets: inherit
